(() => {
  // construyendo la logica una vez el dom haya cargado completamente
  document.addEventListener('DOMContentLoaded', (e) => {
    let total = 0;
    let totalElement = document.getElementById('total');

    totalElement.textContent = "8080"

    alert(nombre);
  });
})();
